# Planning — create tasks after spec readiness

Read the complete human-approved SPEC and engineering contract. Decompose repository changes into independently coherent tasks with explicit dependencies. Use `flow task create`; task identity and lifecycle belong to the CLI, while Git evidence is derived from the canonical task commit. Then prepare the derived execution brief; do not ask for plan approval.
