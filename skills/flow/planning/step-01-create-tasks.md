# Planning — create tasks after spec readiness

Read the complete ready spec and engineering contract. Decompose into independently coherent tasks with explicit dependencies and `traceability: commit|none`. Use `flow task create`; never assign `legacy` to native work. Then prepare the implementation plan and obtain explicit approval before starting a task.
